/**
 * 
 */
package core;

import org.eclipse.core.runtime.ILog;
import org.eclipse.core.runtime.Status;

/**
 * @author Marieta V. Fasie
 * 	marietafasie at gmail dot com
 *
 */
public class PluginLog {
	
	private PluginLog instance;
	
	private ILog logger;
	
	private String pluginName;
	
	public PluginLog(){
		logger = Activator.getDefault().getLog();
		
		pluginName = logger.getBundle().getSymbolicName(); 
	}
	
	public PluginLog getInstance(){
		if(instance == null){
			instance = new PluginLog();
		}
		return instance;			
	}
	
	public void error(String msg, Exception e) {
		logger.log(new Status(Status.INFO, pluginName, Status.OK, msg, e));
	}
	
	public void warning(String msg){
		logger.log(new Status(Status.ERROR,))
	}
}
